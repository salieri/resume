{
  "name": "@faust/release-scripts",
  "version": "0.1.0",
  "description": "Release utilities (LLM-backed release notes, tagging helpers)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && tsc -p ./tsconfig.build.json",
    "clean": "rm -rf ./dist ./coverage ./docs",
    "release:notes": "tsx ./scripts/release-notes/cli.ts",
    "lint": "eslint --ext .js,.ts .",
    "lint:ci": "pnpm run lint -- --quiet",
    "lint:fix": "pnpm run lint --fix",
    "prepublishOnly": "pnpm run build",
    "test": "vitest run",
    "test:ci": "pnpm run test:cov --",
    "test:cov": "pnpm run test -- --coverage",
    "test:watch": "vitest",
    "watch": "tsc -p ./tsconfig.build.json --watch"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@openrouter/sdk": "catalog:",
    "commander": "catalog:",
    "serialize-error": "catalog:"
  },
  "devDependencies": {
    "@faust/eslint-config": "workspace:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "tsx": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
